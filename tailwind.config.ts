import type { Config } from 'tailwindcss'

/**
 * PlantSoul brand extension for Tailwind v4.
 * Design tokens live in globals.css @theme — this file is for plugins only.
 * Brand palette:
 *   ps-black   #050505   near-black background
 *   ps-forest  #0a2e1a   deep forest green (primary)
 *   ps-leaf    #4ade80   bright leaf green (accent)
 *   ps-card    #0d1a10   card surface
 *   ps-border  #1a3a23   default border
 *   ps-text    #e8f5ec   primary text
 *   ps-muted   #6b8f73   muted text
 */
const config: Config = {
  plugins: [],
}

export default config
